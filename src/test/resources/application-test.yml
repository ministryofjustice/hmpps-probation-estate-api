server:
  shutdown: immediate

spring:
  security:
    enabled: false

# Optional: override default security config behavior if needed
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always